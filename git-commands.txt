--> Edit files locally:
- Stage changes:
git add .

- Commit with a message:
git commit -m "Describe what you changed"

- Push changes:
git push



--> (Another person updates the repo)
- Get the latest changes from GitHub:
git pull origin main        # If there are conflicts, Git will mark them in the files, and you’ll need to fix them before committing again.

- After fixing conflicts (if any), commit:
git add .
git commit -m "Resolved merge conflicts"

- Push your changes:
git push


--> Configuring for first time:
git config --global user.name "corethera"
git config --global user.email "harivishnu.corethera@gmail.com"  



--> Create File in github
git remote remove origin
git remote add origin https://github.com/corethera/corethera-ui.git
git branch -M main
git add .
git commit -m "Initial commit"
git push -u origin main



--> Auth Issue Commands:
brew install gh
gh auth login       # login to github with active account
gh auth logout      # logout from account (optional)
